#include <stdio.h>

/**
 * main-prints an alphabet in the lower case
 *
 * Return: Always 0 (Success)
 */

int main(void)
{	/*print alphabets in lower case*/
	char a;

	for (a = 'a'; a <= 'z'; a++)

	putchar(a);
	putchar('\n');

	return (0);
}
