#include <stdio.h>

/**
 * main-print single digit numbers
 * numbers start from 0 and base 10
 *
 * Return: Always 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);

	printf("\n");
	return (0);
}
